"use strict";

var moodio = moodio || {};
moodio.snowglobe = moodio.snowglobe || {};


(function(sg){
    function sgCreate(elem)
    {
        //get options

        //view port details in units
        this.viewport = {};
        this.viewport.width = 0;
        this.viewport.height = 0;
        this.viewport.viewerDistance = 200;
    }

})(moodio.snowglobe);

//run init on load completion
